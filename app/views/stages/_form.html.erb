<%= form_for(@stage) do |f| %>
  <% if @stage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stage.errors.count, "error") %> prohibited this stage from being saved:</h2>

      <ul>
      <% @stage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :journey_id %><br>
    <%= f.number_field :journey_id %>
  </div>
  <div class="field">
    <%= f.label :stageable_id %><br>
    <%= f.number_field :stageable_id %>
  </div>
  <div class="field">
    <%= f.label :stageable_type %><br>
    <%= f.number_field :stageable_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
