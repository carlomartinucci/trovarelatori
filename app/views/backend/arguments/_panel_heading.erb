<div class="panel-heading" id="panel-heading-argument-<%= argument.id %>">

  <span class="pull-right" style="position: relative;">
    <a class="dropdown-toggle" data-toggle="dropdown" href="#" aria-expanded="false">
      <h4>
        <i class="fa fa-bars"></i>
      </h4>
    </a>
    <ul class="dropdown-menu dropdown-user">
      <li>
        <% if @favorite_arguments.pluck(:id).include?(argument.id) %>
          <%= link_to unfavorite_api_v1_argument_path(argument), method: :delete do %>
            <%= fa_icon 'star-o', text: "Rimuovi dai preferiti", class: "fa-fw" %>
          <% end %>
        <% else %>
          <%= link_to favorite_api_v1_argument_path(argument), method: :put do %>
            <%= fa_icon :star, text: "Aggiungi ai preferiti", class: "fa-fw" %>
          <% end %>
        <% end%>
      </li>
      <li>
        <%= link_to '#edit-name', onclick: "$('.toggle_argument_#{argument.id}').toggle()" do %>
          <%= fa_icon :edit, text: "Modifica nome" %>
        <% end if can? :edit, argument %>
      </li>
      <li>
        <%= link_to [:backend, argument], class: "", method: :delete, data: { confirm: 'Are you sure?' } do %>
          <%= fa_icon :trash, text: "Elimina" %>
        <% end if can?(:destroy, argument) %>
      </li>
    </ul>
  </span>


  <%= link_to [:backend, argument] do %>
    <span class="pull-left toggle_argument_<%=argument.id%>">
      <h4>
        <i class="fa fa-arrow-circle-right"></i>
        <%= argument.name %>
        <small>
          <%= argument.theorems.count %> <%= Theorem.model_name.human(count: argument.theorems.count) %>
        </small>
      </h4>
    </span>
  <% end %>

  <span class="pull-left toggle_argument_<%=argument.id%>" style="display: none; width: 60%;">
    <%= form_for [:backend, argument], html: {class: "form-inline"}, remote: true do |f| %>
    <div class="form-group" style="width: 100%;">
      <%= f.text_field :name, class: "form-control", style: "" %>
    </div>
      <%= f.submit "Aggiorna", class: 'btn btn-primary' %>
    <% end %>
  </span>

  <div class="clearfix"></div>
</div>