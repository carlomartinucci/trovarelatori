<%= form_for(@known_topic) do |f| %>
  <% if @known_topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@known_topic.errors.count, "error") %> prohibited this known_topic from being saved:</h2>

      <ul>
      <% @known_topic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :topic_id %><br>
    <%= f.number_field :topic_id %>
  </div>
  <div class="field">
    <%= f.label :knowledge %><br>
    <%= f.text_field :knowledge %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
